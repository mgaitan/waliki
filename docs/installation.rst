============
Installation
============

At the command line::

    $ easy_install django-waliki

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-waliki
    $ pip install django-waliki